// Crie uma função chamada converterEmHoras que receba um número em
// minutos como parâmetro e irá exibir a quantidade em horas.

function converterEmHoras(params) {
    var minutos = 200
    var resultado = (minutos/60) 
    console.log(minutos + "min" + " = " + resultado + "h");
}
converterEmHoras()